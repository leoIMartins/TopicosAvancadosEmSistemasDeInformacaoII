@model IEnumerable<ProjCineWeb.Model.Funcionario>

@{
    ViewData["Title"] = "Index";
}

<h1>Funcionários</h1>

<p>
    <a asp-action="Create">
        <input type="submit" value="Cadastrar Funcionário" class="btn btn-dark" />
    </a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeFuncionario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Atribuicao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Idade)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeFuncionario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Atribuicao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Idade)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">
                    <input type="submit" value="Editar" class="btn btn-secondary" />
                </a>
                <a asp-action="Details" asp-route-id="@item.Id">
                    <input type="submit" value="Detalhes" class="btn btn-info" />
                </a>
                <a asp-action="Delete" asp-route-id="@item.Id">
                    <input type="submit" value="Excluir" class="btn btn-danger" />
                </a>
            </td>
        </tr>
}
    </tbody>
</table>
